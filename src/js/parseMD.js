

const converter = new showdown.Converter(); 

let text = document.getElementsByClassName('question-content')[0];  
console.log(text);  

/*
var converter = new showdown.Converter(),
    text      = '# hello, markdown!',
    html      = converter.makeHtml(text); 

*/ 